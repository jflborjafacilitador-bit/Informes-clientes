<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # React SPA: serve index.html for any route that isn't a real file/folder
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
